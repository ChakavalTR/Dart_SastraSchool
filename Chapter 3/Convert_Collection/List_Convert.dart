void main() {
  //?==================List=====================
  // //!Convert From List to Map Collections
  // List<String> nameList = ['A', 'B', 'C'];
  // Map<int, String> nameMap = nameList.asMap();
  // print(nameMap);

  // //!Convert From List to Set Collections
  List<String> nameList = ['A', 'B', 'C'];
  Set<String> nameSet = nameList.toSet();
  print(nameSet);

  // //!Convert From List to Queue Collections
  // List<String> nameList = ['A', 'B', 'C'];
  // Queue<String> nameQueue = Queue.from(nameList);
  // print(nameQueue);
}
